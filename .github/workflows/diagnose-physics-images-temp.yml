name: Temp Diagnose Physics Images

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Diagnose
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DATABASE_AUTH_TOKEN: ${{ secrets.DATABASE_AUTH_TOKEN }}
        run: npx tsx db/diagnose-physics-images.ts > /tmp/out.txt 2>&1 || echo "FAILED" > /tmp/failed.txt

      - name: Show output
        if: always()
        run: cat /tmp/out.txt || echo "no output captured"

      - name: Post as commit comment
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 -c "
          import json
          status = 'FAILED' if __import__('os').path.exists('/tmp/failed.txt') else 'SUCCESS'
          body = open('/tmp/out.txt').read()[-6000:]
          print(json.dumps({'body': f'Diagnose Physics Images ({status}):\n\n' + body}))
          " > /tmp/payload.json
          curl -s -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/commits/${{ github.sha }}/comments" \
            -d @/tmp/payload.json
